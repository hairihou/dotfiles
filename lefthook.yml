# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-commit:
  jobs:
    - run: deno fmt {staged_files}
      glob: '*.{ts,json,md,toml,yml}'
      stage_fixed: true
