#!/usr/bin/env python3
import os
import sys
from pathlib import Path

sound = (
    Path.home()
    / "dotfiles/public"
    / f"{sys.argv[1] if len(sys.argv) > 1 else 'stop'}.mp3"
)
if sound.exists():
    os.execvp("afplay", ["afplay", str(sound)])
